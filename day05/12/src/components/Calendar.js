import React from 'react';

const Calendar = ({ tasks }) => {
  // A basic representation of a calendar.
  return (
    <div>
      <h2>Calendar</h2>
      <p>Calendar integration would go here.</p>
    </div>
  );
};

export default Calendar;
